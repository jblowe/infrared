# Infrared (containerized): proxy mmap-sites.johnblowe.com → the multi-tenant
# app via Caddy on 127.0.0.1:8081. New vhost (needs a Route 53 CNAME).
<VirtualHost *:80>
  ServerName mmap-sites.johnblowe.com
  ServerAdmin webmaster@localhost

  ProxyPreserveHost On
  ProxyPass        / http://127.0.0.1:8081/
  ProxyPassReverse / http://127.0.0.1:8081/

  ErrorLog  "|/usr/bin/cronolog /var/log/apache2/mmap-sites/%Y/%Y-%m-mmap-sites-error.log"
  CustomLog "|/usr/bin/cronolog /var/log/apache2/mmap-sites/%Y/%Y-%m-mmap-sites-access.log" combined
</VirtualHost>
