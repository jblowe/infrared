.idea
.DS_Store

# app specific stuff
__pycache__/
*.log
*.py[cod]
*.egg-info/
.pytest_cache/

# local image-repo symlinks (per-deployment; created by hand on each host)
/ggm-images
/images
/mmap-images
/mmap-qdrive

# local agent/editor state (preview config, local permissions — host-specific)
.claude/

# host-specific deploy details kept out of git (the repo stays a clean install)
/Caddyfile.ec2
/configs/sites.ec2.toml
/EC2.md
